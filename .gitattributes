# Keep repository text files consistent on every platform.
* text=auto eol=lf

# Windows command scripts should use their native line endings.
*.bat text eol=crlf

# Never apply text conversions to packaged or image assets.
*.jar binary
*.png binary
